#include<stdio.h>
int main(void)
{
	int n,d,e;
	scanf("%d",&n);
	d=((5*n*n)+4);
	e=((5*n*n)-4);
	if((sqrt(d)*sqrt(d))==d || sqrt(e)*sqrt(e)==e)
	 printf("Fibonacci number");
	else
		printf("Not a fibbonacii number");
	return 0;
}
